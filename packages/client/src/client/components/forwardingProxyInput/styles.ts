import { css } from '@emotion/css'

export const styles = {
  container: css({
    width: '100%',
    display: 'flex',
    flexDirection: 'row',
    justifyContent: 'flex-start',
    alignItems: 'center',
    gap: '10px'
  }),
  input: css({
    width: '500px'
  })
}
