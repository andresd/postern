import { css } from '@emotion/css'

export const styles = {
  spinner: css({
    display: 'flex',
    width: '100%',
    height: '100%',
    marginTop: '1rem',
    alignItems: 'center',
    justifyContent: 'center'
  })
}
